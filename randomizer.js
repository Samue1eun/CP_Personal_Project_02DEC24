const randomizer = {
    // randomizes the number that is picked from 1 - 48
    // if the change is negative (-2 to 2) in the last 24 hours returns a random intermediate pose
    // if the change is above positive 2% return an expert pose
    // if the change is less than -2% return a beginner pose
    if ()
}

// https://yoga-api-nzy4.onrender.com/v1/poses?level={beginner, intermediate, or advanced}